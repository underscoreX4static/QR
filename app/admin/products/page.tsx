// Full admin Products page — temporary stub during refactor. A richer version
// (with image upload + batch management) is wired in a follow-up commit.
import ProductsAdminClient from '@/components/admin/ProductsAdminClient'

export const dynamic = 'force-dynamic'

export default function ProductsPage() {
  return <ProductsAdminClient />
}
